---
layout: post
title:  JSF範例 -- 登入 
date:   2010-10-20 23:23:15
categories: java 
tags : [JSF]
---

這篇文章將以一般系統很常見的登入功能來說明JSF是如何實作的。
先看一下這功能的操作流程﹕
輸入網址 http://localhost:8080/login.faces  顯示頁面
2010-10-20_223548
帳號輸入 admin，密碼輸入12345，則導到登入成功頁，否則導到登入失敗頁
2010-10-20_2240482010-10-20_224108
實作

輸入login.faces時，對應出現的頁面為login.xhtml，原始碼如下

{% highlight java %}
&lt;!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"        "
    http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd"&gt;
    &lt;html xmlns="http://www.w3.org/1999/xhtml"
          xmlns:ui=http://java.sun.com/jsf/facelets
          xmlns:h="http://java.sun.com/jsf/html"
          xmlns:f="http://java.sun.com/jsf/core"
          xmlns:c="http://java.sun.com/jsp/jstl/core"&gt;
&lt;f:view contentType="text/html"/&gt;
&lt;h:head&gt;&lt;title&gt;測試&lt;/title&gt;
&lt;/h:head&gt;
&lt;h:body&gt;
&lt;h:form id="loginForm"&gt;
&lt;table&gt;
&lt;tr&gt;&lt;td&gt;帳號﹕&lt;h:inputText id="username"  value="#{loginBean.username}" /&gt;&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;&lt;td&gt;密碼﹕&lt;h:inputSecret   id="password"  value="#{loginBean.password}" /&gt;&lt;/td&gt;&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;h:commandButton id="submit"   action="#{loginBean.login}"  value="登入"/&gt;
&lt;/td&gt;&lt;/tr&gt;
&lt;/table&gt;&lt;/h:form&gt;&lt;/h:body&gt;
&lt;/html&gt;
{% endhighlight %}


在頁面中的loginBean對應到的程式為LoginBean.java，原始碼如下﹕

{% highlight java %}
import javax.faces.bean.ManagedBean; 
import javax.faces.bean.RequestScoped;

@ManagedBean
@RequestScoped  
public class LoginBean implements Serializable {
    String username;  
    String password;
    public void setUsername(String username) {  
        this.username = username;
    }
    public String getUsername() {
        return username; 
    }
    public String getPassword() {  
        return password;
    }
    public void setPassword(String password) {  
        this.password = password;
    }
    public String login() {
        if (username.equals("admin") &amp;&amp; password.equals("12345")) {
            return "login_success";
        } else {
            return "login_fail";
        }  
    }
}
{% endhighlight %}

在按下按扭時，會執行LoginBean裡的login，可以看到回傳值為字串login_success或login_fail，JSF會自動對應到二個檔案login_success.xhtml及login_fail.xhtml，在這二個檔案裡寫上相關訊息，這功能就算完成了。




